"use client";

import { useEffect, useRef } from 'react';

/**
 * SerialTelemetryBridge - Simple manual transmission trigger
 * 
 * This component provides a simple way to manually send test data over serial
 * without any reactive dependencies that could cause infinite loops
 */
export function SerialTelemetryBridge() {
  // This component is now just a placeholder to avoid breaking imports
  // We'll implement manual transmission controls instead
  
  useEffect(() => {
    console.log('SerialTelemetryBridge loaded - manual mode');
  }, []);

  return null;
} 